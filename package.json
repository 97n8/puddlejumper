{
  "name": "puddlejumper",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "vercel-build": "cd n8drive/web && npm ci && npm run build",
    "build": "npm run vercel-build"
  }
}
