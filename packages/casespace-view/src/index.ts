// @pj/casespace-view — headless CaseSpace projection (Issue #101 Visibility Layer).
// Read-only view model over proof-backed runtime tables. Render the thread.

export { projectCaseSpaceView } from './view.js';
export type { CaseSpaceView, CaseSpaceScope } from './view.js';
