PORT=3001
SESSION_SECRET=replace-this-with-a-random-secret
PREVIEW_URL=https://publiclogic.figma.site/
CONTENT_FILE_PATH=./content/prompt.txt
DEPLOY_SCRIPT_PATH=./scripts/deploy.sh
DEPLOY_REPO_ROOT=..
# Deploy script must resolve inside DEPLOY_REPO_ROOT or this app workspace root.

# Optional: override data directory (users/sessions/locks/audit)
# DATA_DIR=./data

# Optional: scan root for attached git repos to show in UI
# REPO_SEARCH_ROOT=..

# Optional: restrict selectable repos in UI (comma-separated absolute or relative paths)
# REPO_ALLOWLIST=../Public_Logic,../pl-poli-case-workspace

# Optional: SharePoint remote credentials (use via env:// refs in environment connection settings)
# TV_GRAPH_TENANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
# TV_GRAPH_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
# TV_GRAPH_CLIENT_SECRET=replace-with-client-secret
