{
  "$name": "PuddleJumper brand tokens",
  "$note": "CANON = the locked /home shell palette (forest green + deep teal-navy). Reviewed shell values, not sampled/invented. One green, one navy.",
  "color": {
    "core": {
      "green": { "value": "#2f5d50", "use": "forest — primary (shell --green)" },
      "navy":  { "value": "#315d74", "use": "deep teal-navy — secondary/accent (shell --blue)" }
    },
    "neutral": {
      "ink":   { "value": "#17211d", "use": "text / monochrome mark" },
      "gold":  { "value": "#b88a3b", "use": "beak / fine accents" },
      "paper": { "value": "#fbfaf6", "use": "surfaces" },
      "bg":    { "value": "#f6f4ed", "use": "app background" }
    },
    "tint": {
      "green10": { "value": "#eef2ef" }
    }
  },
  "type": {
    "wordmark": { "value": "heavy italic rounded sans (outline in production)" },
    "tagline":  { "value": "\"by\" — gray italic; \"PublicLogic\" — bold" }
  }
}
